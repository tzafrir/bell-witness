# docs/ — parent-program records (mirrored)
These four documents are mirrored verbatim from the originating
theory+simulation program ("the parent program"); the only edits are
clearly marked `[Status note added at mirroring time: ...]` lines noting
work that was completed after each document was written.
| File | What it is |
|---|---|
| `t4_impossibility.md` | The theorem forcing L_exp > 0 for the model class — the spine of the whole test. |
| `translation-protocol.md` | The derivation of the L_exp observable and the path from simulation to laboratory. |
| `literature-survey.md` | The prior-art map establishing that the joint-with-witness observable was unmeasured (with arXiv IDs). |
| `proposal-v0.html` | The dedicated-experiment proposal (monitored-decoherence-ancilla Bell test); §7 is the retrospective analysis this repository performed. View raw or via htmlpreview for rendering. |
Not mirrored here: the parent program's full simulation record
(`lemma2-lab-notes.html` v4, ~1 MB with embedded figures, plus the
individual results files it cites). These documents reference it; it is
available from the author on request and will accompany the preprint.
The claims THIS repository stands behind are listed in the main README
("Relation to the parent program"); everything else in these documents
carries the parent program's own evidence.
