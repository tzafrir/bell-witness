# docs/ — parent-program documents

This directory mirrors the parent program's records that carry the claims
this repository cites but does not itself establish (the T4 theorem, the
simulated substrate magnitudes L_exp ≈ 0.25–0.30, the observable
derivation, the dedicated-experiment proposal):

| File | Carries | Status |
|---|---|---|
| `t4_impossibility.md` | the T4 theorem + proof | **to be added by the maintainer** |
| `translation-protocol.md` | derivation of the L_exp observable | to be added |
| `literature-survey.md` | prior-art map with arXiv IDs | to be added |
| `proposal-v0.html` | the dedicated-experiment proposal | to be added |
| `bridging_surface_results.md`, `leak_law_results.md` | measured substrate magnitudes & scaling | to be added |

Until these land, the corresponding statements in the README and reports
should be read as *citations to the parent program*, not as claims this
repository's code re-derives. Everything this repo derives itself is
re-derivable via `python3 reproduce.py`.
